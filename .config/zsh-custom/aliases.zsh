# Replace `ls` with `eza`.
alias ll="eza --icons -w 80"
alias ls="ll -lh"  # list view
alias la="ll -lah" # list all
alias lt="ll -T"   # tree view

# this makes taking rice screenshots easier.
alias neofetch="clear; tabs -4; neofetch"

# only temporary, pls install this properly later.
# alias webcrack="node ~/tools/webcrack/packages/webcrack/dist/cli.js"
